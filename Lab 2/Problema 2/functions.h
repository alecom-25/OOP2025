#pragma once
#include <string>
#include "student.h"

using namespace std;

int CompName(student& name1, student& name2);
int CompMath(student& nota1, student& nota2);
int CompEng(student& nota1, student& nota2);
int CompHist(student& nota1, student& nota2);
int CompAvr(student& nota1, student& nota2);
